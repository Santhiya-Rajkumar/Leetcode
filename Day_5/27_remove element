class Solution {
    public int removeElement(int[] nums, int val) {
       int len=nums.length; 
       int index=0;
      for(int itr=0;itr<len;itr++){
        if(nums[itr]!=val){
            nums[index]=nums[itr];
            index++;
        }
          
      }  
      return index;
    }
}
